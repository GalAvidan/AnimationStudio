# Assets

Store source assets here: SVGs, audio, fonts, screenshots, references, and exported design files.

Use one folder per animation project when assets are project-specific:

```text
assets/<project-name>/
```

For SVG animation, preserve meaningful layer IDs so code and agents can target elements reliably.
